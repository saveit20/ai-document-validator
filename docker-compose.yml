services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      EXTRACTOR: ${EXTRACTOR:-heuristic}
      LLM_TRANSPORT: ${LLM_TRANSPORT:-replay}
